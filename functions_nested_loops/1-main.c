#include "main.h"  // Make sure this line is present

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    print_alphabet();  // The function should now be recognized
    return (0);
}
